const all = require('./all');
const pages = require('./pages');
const sourceAssets = require('./source_assets');
const styles = require('./styles');
const themeAssets = require('./theme_assets');


module.exports = {
  all,
  pages,
  sourceAssets,
  styles,
  themeAssets,
};
